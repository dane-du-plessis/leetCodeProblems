/**
 * @param {number[]} nums1
 * @param {number} m
 * @param {number[]} nums2
 * @param {number} n
 * @return {void} Do not return anything, modify nums1 in-place instead.
 */
var merge = function (nums1, m, nums2, n) {
    for (let i = 0; i < n; i++) {
        // get first element from nums1
        const num = num2[i]
        // insert it in the right position in nums2
        insert(num, nums2, m, n)
    }
};

var insert = function (num, nums2, m, n) {
    // here?
    for (let j = 0; j < m + n; j++) {
    }
}

let nums1 = [1, 2, 3, 0, 0, 0]   // , m = 3
let nums2 = [2, 5, 6]         // , n = 3

